---
platform: linux

image_resource:
  type: docker-image
  source: {repository: hashicorp/terraform}

inputs:
- name: tf-demo

run:
  path: sh
  args:
  - -exc
  - |
    terraform init
    ls -a
    echo heeeeeeey
